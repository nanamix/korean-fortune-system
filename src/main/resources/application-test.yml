spring:
  config:
    activate:
      on-profile: test
  datasource:
    url: jdbc:h2:mem:testdb
    username: test
    password: test
  jpa:
    hibernate:
      ddl-auto: create-drop
    show-sql: false
jwt:
  secret: test-secret-key-for-testing-environment-123456789
  expiration: 300
  refresh-expiration: 600
logging:
  level:
    root: WARN
    com.fortune: INFO 