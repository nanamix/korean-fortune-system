# 이 파일에는 secret을 저장하지 않는다.
#
# 운영 값은 OpenBao KV v2의 아래 경로에서 관리한다.
#   secret/projects/korean-fortune-system/<environment>
#
# 비밀이 아닌 선택자(주소, KV API 경로, bootstrap host directory)는
# docker/docker-compose.openbao.override.yml의 기본값 또는 셸 인자로 지정한다.
