# docker/prometheus/prometheus.yml
global:
  scrape_interval: 15s

scrape_configs:
  - job_name: 'korean-fortune-app'
    static_configs:
      - targets: ['app:18080']
    metrics_path: '/actuator/prometheus'
    scrape_interval: 5s
